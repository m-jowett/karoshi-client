bin_SCRIPTS = manage-flags notify-shutdown set-local-password set-location
CLEANFILES = $(bin_SCRIPTS)
EXTRA_DIST = manage-flags.in notify-shutdown.in set-local-password.in set-location.in

include $(top_srcdir)/subst.mk
